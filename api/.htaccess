<IfModule mod_rewrite.c>
RewriteEngine On
# Pass full Authorization header through to PHP (was ^(.) which only kept 1 char)
RewriteCond %{HTTP:Authorization} ^(.*)$
RewriteRule .* - [E=HTTP_AUTHORIZATION:%1]
</IfModule>
